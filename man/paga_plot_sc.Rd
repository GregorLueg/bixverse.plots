% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_trajectory_plots.R
\name{paga_plot_sc}
\alias{paga_plot_sc}
\title{PAGA abstracted graph over an embedding}
\usage{
paga_plot_sc(
  object,
  paga_res,
  embedding = "umap",
  threshold = 0.01,
  tree_only = FALSE,
  node_colour_by = NULL,
  show_cells = TRUE,
  label = TRUE,
  embd_modality = c("rna", "adt", "wnn"),
  centroid = c("median", "mean"),
  point_size = NULL,
  point_alpha = 0.2,
  raster = NULL,
  raster_dpi = c(512, 512),
  cell_colour = "grey85",
  edge_colour = "grey40",
  edge_width = c(0.2, 3),
  max_node_size = 12,
  palette = NULL,
  label_size = 3,
  label_color = "black",
  label_font = "bold"
)
}
\arguments{
\item{object}{A single cell class.}

\item{paga_res}{\code{PagaRes} class. The output of \code{\link[bixverse:run_paga_sc]{bixverse::run_paga_sc()}},
run on this object.}

\item{embedding}{String. Name of the embedding to position the nodes in.}

\item{threshold}{Numeric. Edges below this connectivity are dropped
(default: 0.01).}

\item{tree_only}{Boolean. Draw the maximum spanning forest rather than the
full abstracted graph (default: FALSE).}

\item{node_colour_by}{Optional string. A numeric obs column summarised per
cluster, e.g. \code{"palantir_pseudotime"}, giving continuously coloured nodes.
\code{NULL} (default) colours the nodes discretely by cluster.}

\item{show_cells}{Boolean. Draw the cells underneath the graph
(default: TRUE).}

\item{label}{Boolean. Label the nodes with their cluster (default: TRUE).}

\item{embd_modality}{String. One of \code{c("rna", "adt", "wnn")}. Modality the
embedding is pulled from.}

\item{centroid}{String. One of \code{c("median", "mean")}. How a cluster's
position is summarised. Median by default, since embeddings throw stragglers
that drag a mean off its cluster.}

\item{point_size}{Optional numeric. Size of the cells. If not provided, will
be auto-determined.}

\item{point_alpha}{Numeric. Alpha of the cells (default: 0.2).}

\item{raster}{Optional boolean. Shall the cell layer be rasterised. If \code{NULL}
and the number of cells is larger than \code{1e5}, defaults to TRUE.}

\item{raster_dpi}{Two numerics. Pixel resolution for rasterized plots, passed
to geom_scattermore(). Default is \code{c(512, 512)}.}

\item{cell_colour}{String. Colour of the cell layer (default: "grey85").}

\item{edge_colour}{String. Colour of the edges (default: "grey40").}

\item{edge_width}{Two numerics. Range the edge widths are scaled into
(default: \code{c(0.2, 3)}).}

\item{max_node_size}{Numeric. Size of the largest node. Node \emph{area} tracks
the cell count, which is what people read off it (default: 12).}

\item{palette}{Optional string. Palette for the nodes, see \code{\link[=bx_colors]{bx_colors()}}.
\code{NULL} (default) resolves to \code{"main"} for the discrete case and
\code{"sequential"} when \code{node_colour_by} is given.}

\item{label_size}{Numeric. Size of the labels.}

\item{label_color}{String. Colour of the labels.}

\item{label_font}{String. Font of the labels.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object.
}
\description{
Draws the PAGA graph with every cluster sitting at the centroid of its cells
in an embedding, over a faint scatter of the cells themselves. A free layout
of the abstracted graph puts the nodes somewhere arbitrary, which the reader
then has to relate back to the embedding by hand. This does not.

The abstracted graph is close to complete on real data, so \code{threshold} is
doing real work and dropping it to zero gives a hairball. \code{tree_only} is the
shortcut to the backbone.
}
\references{
Wolf, et al., Genome Biol., 2019.
}
