% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_ext.R
\name{label_centroids}
\alias{label_centroids}
\title{Label Centroids in Scatter Plots}
\usage{
label_centroids(
  data = NULL,
  label_by,
  colour = "black",
  fill = "white",
  alpha = 0.5,
  label.size = 0,
  size = 4,
  fontface = "bold",
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} containing the scatter
plot points. If \code{NULL}, the data from the parent ggplot is used. Must
contain the columns referenced by the parent plot's x/y aesthetics and by
\code{label_by}.}

\item{label_by}{Character. Name of the (discrete) column to group and label
by.}

\item{colour}{Text colour. Default: \code{"black"}.}

\item{fill}{Box fill colour. Default: \code{"white"}.}

\item{alpha}{Box fill transparency in \code{[0, 1]}. Default: \code{0.5}.}

\item{label.size}{Box border line width in mm. Set to \code{0} to hide the
border. Default: \code{0}.}

\item{size}{Text size in mm. Default: \code{4}.}

\item{fontface}{Font face. Default: \code{"bold"}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2]{geom_label}}.}
}
\value{
A ggplot layer.
}
\description{
Adds boxed text labels at the centroid position of each group in a scatter
plot. Computes group centroids with data.table for efficient summarisation.
Useful for labelling cluster centres in embedding or dimensionality
reduction plots (UMAP, t-SNE, PCA).
}
\examples{
\dontrun{
embedding_plot_sc(
  object = sc_object,
  embedding = "umap",
  colour_by = "donor_id"
) +
  label_centroids(label_by = "donor_id")
}

}
