% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blitzgsea_plots.R
\name{plot_blitzgsea_es_null}
\alias{plot_blitzgsea_es_null}
\title{Plot observed enrichment scores against the blitzGSEA null}
\usage{
plot_blitzgsea_es_null(
  null_model,
  res,
  pathways_of_interest,
  n_points = 512L,
  text_size = 3
)
}
\arguments{
\item{null_model}{\code{BlitzGseaNull} object from
\code{\link[bixverse:blitzgsea_calibrate]{bixverse::blitzgsea_calibrate()}}. Has to be the null the results were
scored against.}

\item{res}{data.table. Output of \code{\link[bixverse:calc_blitzgsea]{bixverse::calc_blitzgsea()}}. Needs the
columns \code{c("pathway_name", "es", "size", "pvals", "fdr")}.}

\item{pathways_of_interest}{String vector. Names of the pathways to plot.
These need to be represented in \code{res$pathway_name}.}

\item{n_points}{Integer. Resolution of the density curve. Defaults to \code{512L}.}

\item{text_size}{Numeric. Size of the annotation text. Defaults to \code{3}.}
}
\value{
A named list of \code{ggplot} objects, one per element of
\code{pathways_of_interest}.
}
\description{
For each pathway of interest, draws the fitted null density at that
pathway's size and drops the observed enrichment score on top, with the tail
beyond it shaded. This is where the p-value comes from: blitzGSEA never
permutes per pathway, it reads the score off the gamma tail interpolated to
the set size.

The raw permutation scores never cross the Rust boundary, so this is the
fitted density, not a histogram of null draws.
}
\references{
Lachmann, et al., Bioinformatics, 2022
}
