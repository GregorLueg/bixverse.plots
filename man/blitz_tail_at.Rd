% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blitzgsea_plots.R
\name{blitz_tail_at}
\alias{blitz_tail_at}
\title{Interpolate the blitzGSEA gamma parameters at a given gene set size}
\usage{
blitz_tail_at(null_model, size)
}
\arguments{
\item{null_model}{\code{BlitzGseaNull} object from
\code{\link[bixverse:blitzgsea_calibrate]{bixverse::blitzgsea_calibrate()}}.}

\item{size}{Numeric. The gene set size to evaluate at.}
}
\value{
A named list with \code{shape_pos}, \code{scale_pos}, \code{shape_neg},
\code{scale_neg} and \code{pos_ratio}, the last one clamped to \verb{[0, 1]}.
}
\description{
Mirrors \code{interp_log_linear_at()} on the Rust side: bracket on the anchor
grid, then interpolate linearly in log space. Note that the bracket index is
clamped to the interior, so values outside the grid are \emph{extrapolated} along
the terminal segment rather than held flat. That is deliberate,
\code{\link[stats:approx]{stats::approx()}} with \code{rule = 2} would clamp instead and quietly disagree
with the p-values the scoring returned.
}
\keyword{internal}
