% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_plots.R
\name{feature_scatter_plot_sc}
\alias{feature_scatter_plot_sc}
\title{Scatter / hex plot of two features against each other}
\usage{
feature_scatter_plot_sc(
  object,
  feature_1,
  feature_2,
  geom = c("density", "hex"),
  remove_zeros = TRUE,
  smoothness = 10,
  bins = 60,
  modality = c("rna", "adt"),
  point_size = 2.5,
  point_alpha = 0.5,
  raster = NULL,
  raster_dpi = c(512, 512)
)
}
\arguments{
\item{object}{A single cell class.}

\item{feature_1}{String. First feature (x-axis), optionally \verb{_rna} / \verb{_adt}
suffixed.}

\item{feature_2}{String. Second feature (y-axis), optionally \verb{_rna} / \verb{_adt}
suffixed.}

\item{geom}{String. \code{"density"} or \code{"hex"} (default: \code{"density"}).}

\item{remove_zeros}{Boolean. Drop cells where both features are zero
(default: TRUE).}

\item{smoothness}{Numeric. Bandwidth multiplier for the KDE (default: 10).
Only used for \code{geom = "density"}.}

\item{bins}{Numeric. Number of hex bins (default: 60). Only used for
\code{geom = "hex"}.}

\item{modality}{String. Fallback modality for unsuffixed features. One
of \code{c("rna", "adt")}.}

\item{point_size}{Numeric. Point size for density plots (default: 2.5).}

\item{point_alpha}{Numeric. Alpha for density plots (default: 0.5).}

\item{raster}{Optional boolean. Shall the plot be rasterised. If \code{NULL}
and \code{n_cells > 1e5}, defaults to TRUE. Only applies to
\code{geom = "density"}.}

\item{raster_dpi}{Two numerics. Pixel resolution for rasterised plots
(default: \code{c(512, 512)}).}
}
\value{
A \code{\link[ggplot2]{ggplot}} object.
}
\description{
Plots two features against each other (typical use case: protein vs mRNA
on ADT/RNA data). Each feature may carry a \verb{_rna} or \verb{_adt} suffix to
select its modality independently; unsuffixed features fall back to
\code{modality}. With \code{geom = "density"} points are coloured by 2D KDE, with
\code{geom = "hex"} cells are binned into hexagons.
}
